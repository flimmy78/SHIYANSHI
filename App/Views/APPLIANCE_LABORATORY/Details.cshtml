@model Langben.DAL.APPLIANCE_LABORATORY

@{
    Layout = "~/Views/Shared/Details.cshtml";
}
@using Common
 @section CurentPlace {
    详细  
}
 
    <fieldset>
        <legend>
            <input class="a2 f2" type="button" onclick="window.location.href = 'javascript:history.go(-1)';" value="返回" />
        </legend>
        <div class="bigdiv">
                    
                <div class="display-label">
                      @Html.LabelFor(model => model.UNDERTAKE_LABORATORYID) :
                </div>
                <div class="display-field">
                    @if (Model.UNDERTAKE_LABORATORY != null)
                       {
                    @Model.UNDERTAKE_LABORATORY.NAME
                    } 
                </div>        
                <div class="display-label">
                      @Html.LabelFor(model => model.APPLIANCE_DETAIL_INFORMATIOID) :
                </div>
                <div class="display-field">
                    @if (Model.APPLIANCE_DETAIL_INFORMATION != null)
                       {
                    @Model.APPLIANCE_DETAIL_INFORMATION.BAR_CODE_NUM
                    } 
                </div>        
                <div class="display-label">
                      @Html.LabelFor(model => model.PREPARE_SCHEMEID) :
                </div>
                <div class="display-field">
                    @if (Model.PREPARE_SCHEME != null)
                       {
                    @Model.PREPARE_SCHEME.REPORT_CATEGORY
                    } 
                </div>
        </div>
    </fieldset>

<script type="text/javascript">

    $(function () {
        // 获取内容并显示
        var id = '@ViewBag.Id';
        $.ajax({
            url: "/api/APPLIANCE_LABORATORYApi/" + id,
            type: "POST",
            // async: false,
            success: function (res) {
		
            }
        })
    });
</script>

